<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand"  href="#">DoctIN</a>
    </div>
    <% if logged_in? %>
      <div class="nav_item center">
          <%= link_to "Home",doctor_path(same_current_user),class: "btn btn-log btn-success" %>
        </div>
     <div class="nav_item center">
          <%= link_to "News Feed",posts_path,class: "btn btn-log btn-success" %>
        </div>
    <% end %>
      <ul class="nav navbar-right nav_it down  center">
          <% if !logged_in? %>
         <p> <%= link_to "SignIn",login_path,class:"btn btn-info" %> 
           <% end %>
          <% if logged_in? %>
              <p> <%= link_to "SignOut",logout_path,class:"btn btn-warning btn-small" %> 
              </p>
            
          <% end %>
      </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>